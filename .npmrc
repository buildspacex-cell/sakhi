enable-pre-post-scripts=true
ignore-scripts=false
# Prevent styled-jsx from being hoisted to root where it picks up React 19
# (fixes styled-jsx useContext crash on Vercel 404/500 static page generation)
hoist-pattern[]=!styled-jsx
hoist-pattern[]=!styled-jsx/**
