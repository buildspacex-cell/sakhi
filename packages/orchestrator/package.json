{
  "name": "@sakhi/orchestrator",
  "version": "0.1.0",
  "description": "Conversation orchestrator wiring messages through facets, context, planner, and actions via the event bus.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@sakhi/contracts": "workspace:*",
    "@sakhi/event-bus": "workspace:*",
    "@sakhi/context-builder": "workspace:*",
    "@sakhi/planner": "workspace:*"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3"
  }
}
